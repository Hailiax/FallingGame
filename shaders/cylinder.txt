uniform float time;
uniform vec2 resolution;

varying vec3 vPosition;
varying vec2 vUv;
  
void main()	{
    float value = fbm(vec4(vPosition.x, vPosition.y, vPosition.z, time/1000.));
    gl_FragColor = vec4(value);
}

